analysis:
  enabled: true
  obfuscation_level: minimal
  provider: ollama
  ollama:
    # These are FALLBACKS only - compose env vars override these
    url: http://ollama:11434  
    model: llama3.1:8b
